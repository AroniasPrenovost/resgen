# The generator's private, local-only state / history / learnings.
# These are the bot's memory — they stay on your machine, out of the repo.
personas.json
topics_log.json
history.json
state.json

# Ready-to-paste social captions the author copies out by hand. Local only.
social_drafts/

# transient
generator.log
_selftest_page.tsx
__pycache__/
*.pyc
